services:
  - type: web
    name: desgrabador-youtube
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT --workers 1 --timeout 180 youtubesubs_app:app
    autoDeployTrigger: commit
    healthCheckPath: /
